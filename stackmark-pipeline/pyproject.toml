[project]
name = "stackmark-pipeline"
version = "0.1.0"
description = "StackMark — Barebone ingestion pipeline prototype"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "requests",
    "openai>=2.29.0",
]

[project.scripts]
stackmark = "pipeline:main"
