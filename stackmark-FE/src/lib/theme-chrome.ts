/**
 * Colors for HTML / manifest / icons where CSS variables are not available.
 * Keep in sync with `--navy` in `src/styles/theme.css` and `public/manifest.json`.
 */
export const THEME_COLOR_NAVY = "#0a1929" as const;
